---
# Default mesh status
mesh_enabled: false

# Empty lists make an unconfigured role a no-op. Every configured entry must
# supply a stable named id; wifi interfaces also supply a semantic `find` map.
wireless_devices: []
wireless_interfaces: []
